/* import React from 'react';
import {jest} from '@jest/globals';

import App from './App';
import { renderer } from '@testing-library/react-native'

it('should render a dialog', () => {
const renderedComponent = render(<App />); 
    
})

//display a dialog
// fails if the dialog is not visible
 */